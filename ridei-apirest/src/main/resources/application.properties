spring.application.name=ridei-apirest

# =================================
# Configuraci� de la base de dades
# =================================
spring.datasource.url=jdbc:mysql://localhost:3307/db-ridei-app?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=ridei_adm
spring.datasource.password=sasa1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =================================
# Configuraci� JPA / Hibernate
# =================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.sql.init.mode=always

# =================================
# Swagger Configuration
# =================================
springdoc.swagger-ui.path=/docs
springdoc.api-docs.path=/api-docs

# =================================
# Logging configuration
# =================================
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [CID:%X{correlationId}] [%thread] %logger{36} - %msg%n

# =================================
# Servidor embeded
# =================================
server.port=8080
